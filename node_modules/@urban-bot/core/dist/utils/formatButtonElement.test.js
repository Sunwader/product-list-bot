"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable react/jsx-key */
const react_1 = __importDefault(require("react"));
const formatButtonElement_1 = require("./formatButtonElement");
const ButtonGroup_1 = require("../components/ButtonGroup");
const array_flatten_1 = require("array-flatten");
describe('formatButtonElement', () => {
    it('return right response for one button', () => {
        const text = 'test name';
        const id = String(Math.random());
        const onClick = () => { };
        const element = (react_1.default.createElement(ButtonGroup_1.Button, { onClick: onClick, id: id }, text));
        expect(formatButtonElement_1.formatButtonElement(element)).toEqual([{ text, onClick, id }]);
    });
    it('return right response for two buttons', () => {
        const text1 = 'test name';
        const text2 = 'test name2';
        const id1 = String(Math.random());
        const id2 = String(Math.random());
        const onClick1 = () => { };
        const onClick2 = () => { };
        const element = [
            react_1.default.createElement(ButtonGroup_1.Button, { onClick: onClick1, id: id1 }, text1),
            react_1.default.createElement(ButtonGroup_1.Button, { onClick: onClick2, id: id2 }, text2),
        ];
        expect(formatButtonElement_1.formatButtonElement(element)).toEqual([
            { text: text1, onClick: onClick1, id: id1 },
            { text: text2, onClick: onClick2, id: id2 },
        ]);
    });
    it('add additional props to response', () => {
        const text = 'test name';
        const onClick = () => { };
        const id = String(Math.random());
        const customProp = true;
        const element = (react_1.default.createElement(ButtonGroup_1.Button, { onClick: onClick, customProp: customProp, id: id }, text));
        expect(array_flatten_1.flatten(formatButtonElement_1.formatButtonElement(element))[0].customProp).toBe(customProp);
    });
    it('add random id by default', () => {
        const text = 'test name';
        const element = react_1.default.createElement(ButtonGroup_1.Button, { onClick: () => { } }, text);
        expect(array_flatten_1.flatten(formatButtonElement_1.formatButtonElement(element))[0].id).toEqual(expect.any(String));
    });
    it('throw error if passed not Button', () => {
        function Text(_props) {
            return null;
        }
        const text = 'test name';
        const element = react_1.default.createElement(Text, null, text);
        expect(() => formatButtonElement_1.formatButtonElement(element)).toThrowErrorMatchingSnapshot();
    });
});
//# sourceMappingURL=formatButtonElement.test.js.map